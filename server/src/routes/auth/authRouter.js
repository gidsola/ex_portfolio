
import { Router } from 'express';

export default Router()

  /**
   * auth login
   */
  .post('/login', (_, res) => {
    res.json(
      //do stuff
    );
  })

  /**
   * auth logout
   */
  .post('/logout', (_, res) => {
    res.json(
      //do stuff
    );
  })
